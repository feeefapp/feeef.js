package-lock=false
# Non-interactive publish: set NPM_TOKEN (granular token with Bypass 2FA).
# Never commit a real token — only the ${NPM_TOKEN} placeholder.
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
